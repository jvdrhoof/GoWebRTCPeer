{
    "version": "2.0.0",
    "type": "shell",
    "tasks": [
        {
            "label": "Build peer",
            "command": [
                "go build -o ../peer.exe"
            ],
            "group": "build",
            "options": {
                "cwd": "${workspaceFolder}/peer"
            }
        }
    ],
}